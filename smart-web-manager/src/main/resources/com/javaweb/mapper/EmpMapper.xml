<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.javaweb.mapper.EmpMapper">
    <!--    新增员工，useGeneratedKeys="true" keyProperty="id" 用了xml写sql，就不能再使用注解-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into emp
        values (null, #{username}, #{password}, #{name}, #{gender}, #{phone}, #{job}, #{salary}, #{image},
                #{entryDate}, #{deptId}, #{createTime}, #{updateTime})
    </insert>
    <!--更新员工-->
    <update id="update">
        update emp
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="name != null">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="job != null">job = #{job},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="image != null">image = #{image},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>


    <!--    批量删除员工-->
    <delete id="deleteBatch">
        delete from emp where id in
        <foreach collection="ids" separator="," item="id" open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <!--    条件查询-->
    <select id="list" resultType="com.javaweb.entity.Emp">
        select e.*, d.name deptName
        from emp e
        left join dept d on e.dept_id = d.id
        <where>
            <if test="name != null and name != ''">
                e.name like concat('%',#{name},'%')
            </if>
            <if test="gender != null">
                and e.gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and e.entry_date between #{begin} and #{end}
            </if>
        </where>
        order by e.update_time desc
    </select>

    <!--resultMap标签，进行一对多数据映射 autoMapping设置为true可以进行自动映射-->
    <resultMap id="empResultMap" type="com.javaweb.entity.Emp" autoMapping="true">
        <!--       id标签，主键映射 -->
        <id column="id" property="id"></id>
        <!--       collection标签：用来封装集合数据，适用于一对多的情况 -->
        <collection property="exprList" ofType="com.javaweb.entity.EmpExpr">
            <result column="ee_id" property="id"></result>
            <result column="ee_empId" property="empId"></result>
            <result column="ee_begin" property="begin"></result>
            <result column="ee_end" property="end"></result>
            <result column="ee_company" property="company"></result>
            <result column="ee_job" property="job"></result>
        </collection>
    </resultMap>
    <select id="getById" resultMap="empResultMap">
        select e.*,
               ee.id      ee_id,
               ee.emp_id  ee_empId,
               ee.begin   ee_begin,
               ee.end     ee_end,
               ee.company ee_company,
               ee.job     ee_job
        from emp e
                 left join emp_expr ee on e.id = ee.emp_id
        where e.id = #{id}
    </select>

    <!-- 查询所有员工-->
    <select id="getAll" resultType="com.javaweb.entity.Emp">
        select *
        from emp
    </select>

    <!--  登录-->
    <select id="selectByUsernameAndPassword" resultType="com.javaweb.entity.Emp">
        select *
        from emp
        where username = #{username}
          and password = #{password}
    </select>

    <!--    <resultMap id="empAllResultMap" type="com.javaweb.entity.Emp" autoMapping="true">-->
    <!--        <id column="id" property="id"></id>-->
    <!--    </resultMap>-->
    <!--    <select id="getAll" resultMap="empAllResultMap">-->
    <!--        select * from emp-->
    <!--    </select>-->
    
    <!-- 根据ID和旧密码查询员工 -->
    <select id="getByIdAndOldPassword" resultType="com.javaweb.entity.Emp">
        select * from emp where id = #{id} and password = #{oldPassword}
    </select>
    
    <!-- 根据ID更新密码 -->
    <update id="updatePasswordById">
        update emp set password = #{password} where id = #{id}
    </update>

</mapper>