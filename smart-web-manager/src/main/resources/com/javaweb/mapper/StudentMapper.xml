<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.javaweb.mapper.StudentMapper">
    <!--保存学生-->
    <insert id="save">
        insert into student(id, name, no, gender, phone, id_card, is_college, address, degree, graduation_date,
                            clazz_id, create_time, update_time)
        values (null, #{name}, #{no}, #{gender}, #{phone}, #{idCard},
                #{isCollege}, #{address}, #{degree}, #{graduationDate},
                #{clazzId}, #{createTime}, #{updateTime})
    </insert>
    <!--修改学生-->
    <update id="update">
        update student
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="no != null">
                no = #{no},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="idCard != null">
                id_card = #{idCard},
            </if>
            <if test="isCollege != null">
                is_college = #{isCollege},
            </if>
            <if test="address != null">
                address = #{address},
            </if>
            <if test="degree != null">
                degree = #{degree},
            </if>
            <if test="graduationDate != null">
                graduation_date = #{graduationDate},
            </if>
            <if test="clazzId != null">
                clazz_id = #{clazzId},
            </if>
            <if test="violationCount != null">
                violation_count = #{violationCount},
            </if>
            <if test="violationScore != null">
                violation_score = #{violationScore},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>
    <!--违纪扣分-->
    <update id="violation">
        update student
        set violation_count = violation_count + 1,
            violation_score = violation_score + #{score}
        where id = #{id}
    </update>
    <!--批量删除学生-->
    <delete id="deleteBatch">
        delete from student where id in
        <foreach collection="ids" separator="," item="id" open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <!--列表分页查询-->
    <select id="page" resultType="com.javaweb.entity.Student">
        select *
        from student
        <where>
            <if test="name != null and name != ''">
                name like concat('%' ,#{name},'%')
            </if>
            <if test="degree != null and degree != ''">
                and degree = #{degree}
            </if>
            <if test="clazzId != null and clazzId != ''">
                and clazz_id = #{clazzId}
            </if>
        </where>
        order by update_time desc
    </select>
    <!--根据ID查询-->
    <select id="getById" resultType="com.javaweb.entity.Student">
        select *
        from student
        where id = #{id}
    </select>
</mapper>
